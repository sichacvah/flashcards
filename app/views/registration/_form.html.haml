= simple_form_for user, url: registration_index_path, method: :post, html: { class: 'form-horizontal' } do |f|
  = f.input :email
  = f.input :password, label: t(:password)
  = f.input :password_confirmation, label: t(:password_confirmation)
  = f.button :submit
